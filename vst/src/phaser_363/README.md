# Phaser363

Bundled DPF VST3 for Rocksmith `Pedal_Phaser`.

The local reference is `pedals/phaser 363.png`, an MXR Phase 90 style
four-stage JFET phaser. Rocksmith exposes only `Rate`, so the depth, feedback,
and wet/dry blend are voiced internally.
