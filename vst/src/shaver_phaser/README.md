# ShaverPhaser

Bundled DPF VST3 for Rocksmith `Pedal_ShaverPhaser`.

The local reference is `pedals/shaver phaser.jpeg`, an ET-25B style phaser
with Rate and Depth controls. Rocksmith exposes those same two knobs, so the
phase blend and resonance are fixed internally.
